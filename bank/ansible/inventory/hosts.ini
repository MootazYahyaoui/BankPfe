# Personnaliser : copier depuis hosts.example.ini ou décommenter une des options ci-dessous.

[bank_ci]
# VM distante (exemple) :
# bank-ci ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

# Même machine que le contrôleur (WSL ou la VM elle-même) :
localhost ansible_connection=local
