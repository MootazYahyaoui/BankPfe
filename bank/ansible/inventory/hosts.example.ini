# Copier vers hosts.ini et adapter l'IP / l'utilisateur SSH.
# Exemple : VM Ubuntu où tournent Jenkins + Minikube.

[bank_ci]
# my-ci-vm ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[bank_ci:vars]
# ansible_python_interpreter=/usr/bin/python3
